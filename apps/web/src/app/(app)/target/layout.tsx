import type { Metadata } from "next";

export const metadata: Metadata = { title: "Target & Capaian - Kesling Cirebon" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
