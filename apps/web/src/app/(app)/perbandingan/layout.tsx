import type { Metadata } from "next";

export const metadata: Metadata = { title: "Perbandingan - Kesling Cirebon" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
